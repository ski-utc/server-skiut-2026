##### Done 
- Ajouter un bouton annuler ou rafraichier en plus du bouton "Retour" sur le ErrorScreen. Ce bouton refresh la page sur laquelle on est pour la reset complètement
- Fermer la modal perm si clique sur le planning, 
- ajouter du word wrap sur le title de la modal planning, 
- eviter que la background muted glisse avec la modal, 
- bug sur les likes et warn offline qui partent en négatif, 
- bug sur la navigation dans valideDefisScreen : si je suis dans le menu "en attente" -> je clique sur un défi -> je reviens en arrière, je suis encore sur l'onglet "défis en attente", mais c'est tous les défis qui apparaissent (sans filtre), idem sur valideAnecdoteScreen et notification 
- Mode rapide sur les anecdotes pas encore validees comme pour les défis, 
- Proposer la création d'une permanence que quand tous les champs sont remplis 
- app qui plante (après le login en arrivant sur le homeScreen, quand on remonte le volet de notification, quand on upload une video)
- Faire une passe d'optimisation partout sur l'app (navigation, chargement, cache, memo, callback, ...)
- Définir des interfaces/types partout où c'est nécessaire dans l'app et passer npm run lint sans warning
- Ne plus avoir de type any et tout typer (sauf pour les erreur comme on ne semble pas pouvoir faire string[])
- Clean des lib deprecated
Limiter les réponses serveur à ce que les interfaces react ont besoin



### Avant de continuer 
- Faire les TODO et autres commentaires notés
- Verifier que tout fonctionne 
  - bug sur l'envoie des defis: envoie de defis qui est considéré en pending puis n'update pas le state du screen précédent (et ne reste pas en mémoire, il faudrait peut etre modifier le cache de apiGet avec le nouveau défi)
  - Skinder   
  - tournée des chambres
  - admin
- Verifier le mode hors ligne 
- veirifer le vitesseDeGlisse en background 

- Envoyer une et une seule alerte si la personne est déconnecter (attention comme plusieurs requêtes se lancent au logout, il va sûrement falloir retirer la désactivation de token push au logout pour ne pas avoir plusieurs alert d'un coup), 
- Modifier les écrans de login pour détailler d'avantage les features
- Debug notification



##### TODO fast
- Modifier activities et planning pour n'avoir que 2 champs start et end en dateTime au cas où un évent dure jusqu'au lendemain
  - Adapter migration, models, controllers, seeder, factory, test, back-office et utilisation côté app



##### Verification finales à la main
- [ ] vérifier les notifications 
  - [ ] save des token
  - [ ] classique (push)
  - [ ] skinder
  - [ ] monoprut
  - [ ] perms
  - [ ] lue / non lue
- [ ] vérifier schedule de notification des permanences



########### Pas de deadline ##########
- [~] Update la charte RGPD, l'export, anonymisation et suppression des données
  - updater / verifier les fonctions du RGPD controller
- [~] Vérifier que l'uml est correct
- [ ] mise en prod 
- [ ] weathrAPI Key en secret (et autres config idem, vérifier que les .env sont clean)
- [ ] réactiver tous les healthcheck laravel
